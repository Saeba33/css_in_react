import styled from "styled-components";

const NavBar = () => {
    return (
        <div id="NavBarContainer">
            <h1>NavBar</h1>

            <p>NavBar Page</p>
        </div>
    );
};

export default NavBar;