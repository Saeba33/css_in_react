import styled from "styled-components";

const Card = () => {
    return (
        <div id="CardContainer">
            <h1>Card</h1>

            <p>Card Page</p>
        </div>
    );
};

export default Card;